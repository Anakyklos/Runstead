.env.local
/runstead

# Local agent workspace artifacts (never part of the repository)
/.omx/
/CLAUDE.md

# Local agent scratch (never part of the repository)
/scratch/

# Python bytecode and bytecode caches (never part of the repository)
__pycache__/
*.py[cod]
